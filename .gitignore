# OS / editor cruft
.DS_Store
Thumbs.db

# local-only Claude Code settings (machine-specific permissions, not for the repo)
.claude/settings.local.json

# node (not used by the game, but just in case)
node_modules/
